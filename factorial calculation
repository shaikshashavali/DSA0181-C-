#include<iostream>
using namespace std;
int main() {
    int n;
    unsigned long long fact = 1;
    cout << "Enter a number: ";
    cin >> n;
    while (n > 0) {
        fact *= n;
        n--;
    }
    cout << "Factorial is " << fact << endl;
    return 0;

}
